digraph M3 {
ost-benchmark interpretation layers",  graph [
    rankdir=TB,
    labelloc=t,
    label="P
    fontname="Helvetica",
    fontsize=18,
    pad=0.35,
    nodesep=0.28,
    ranksep=0.52,
    splines=polyline,
    bgcolor="white",
    compound=true
  ];

  node [
    shape=box,
    style="rounded,filled",
    fontname="Helvetica",
    fontsize=10,
    color="#264653",
    fillcolor="#F7FBFF",
    margin="0.14,0.09"
  ];

  edge [
    color="#5C677D",
    penwidth=1.3,
    arrowsize=0.8,
    fontname="Helvetica",
    fontsize=9
  ];

  upstream [
    label="Collect frozen benchmark outputs",
    fillcolor="#E9F5F2"
  ];

  subgraph cluster_explain {
    label="Explainability layer";
    color="#9AD1D4";
    style="rounded";

    e1 [label="Apply family-matched explainability methods", fillcolor="#F2FBFB"];
    e2 [label="Assign features to block taxonomy", fillcolor="#F2FBFB"];
    e3 [label="Consolidate block-level importance scores", fillcolor="#F2FBFB"];

    e1 -> e2 -> e3 [weight=10];
  }

  subgraph cluster_calibration {
    label="Calibration layer";
    color="#F4A261";
    style="rounded";

    c1 [label="Collect calibration evidence", fillcolor="#FFF8F1"];
    c2 [label="Audit calibration coherence by horizon", fillcolor="#FFF8F1"];

    c1 -> c2 [weight=10];
  }

  takeaway [
    shape=note,
    label="Translate scores into feature and calibration signals",
    fillcolor="#FFFDEB",
    color="#B08900"
  ];

  upstream -> e1 [lhead=cluster_explain];
  upstream -> c1 [lhead=cluster_calibration];

  e3 -> takeaway [ltail=cluster_explain];
  c2 -> takeaway [ltail=cluster_calibration];
}
