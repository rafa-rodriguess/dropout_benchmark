digraph M1 {
  graph [
    rankdir=TB,
    labelloc=t,
    label="Data preparation pipeline",
    fontname="Helvetica",
    fontsize=18,
    pad=0.3,
    nodesep=0.22,
    ranksep=0.6,
    splines=ortho,
    bgcolor="white"
  ];

  node [
    shape=box,
    style="rounded,filled",
    fontname="Helvetica",
    fontsize=10,
    color="#264653",
    fillcolor="#F7FBFF",
    margin="0.12,0.08"
  ];

  edge [
    color="#5C677D",
    penwidth=1.3,
    arrowsize=0.8,
    fontname="Helvetica",
    fontsize=9
  ];

  subgraph cluster_raw {
    label="Raw OULAD sources";
    color="#C8D5E0";
    style="rounded";
    raw_admin [label="Load administrative records", fillcolor="#FFF7E6"];
    raw_learning [label="Load learning activity records", fillcolor="#FFF7E6"];
  }

  outcome [label="Define dropout outcome", fillcolor="#E9F5F2"];
  features [label="Construct feature blocks", fillcolor="#E9F5F2"];
  split [label="Partition into train and test sets", fillcolor="#E9F5F2"];

  subgraph cluster_dynamic {
    label="Family A — Dynamic Weekly";
    color="#9AD1D4";
    style="rounded";
    dyn [label="Build weekly person-period rows", fillcolor="#F2FBFB"];
  }

  subgraph cluster_comparable {
    label="Family B — Static Early-Window";
    color="#F4A261";
    style="rounded";
    comp [label="Build enrollment-level feature vectors", fillcolor="#FFF8F1"];
  }

  raw_admin -> outcome;
  raw_learning -> outcome;
  outcome -> features;
  features -> split;
  split -> dyn;
  split -> comp;
}
